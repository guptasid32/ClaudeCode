"""Operator tools — CLIs for stress-testing, backtesting, benchmark loading.

Each tool is a thin orchestration layer over the engines and ingestion
modules. They are intended to be invoked as `python -m india_monthly_alpha_engine.tools.<name>`.
"""
